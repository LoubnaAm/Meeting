package connection;




public class dbConnection {
   /* public static  Connection connecte() throws SQLException {
        try {
            String url = "";
            String user = "root";
            String password = "";
            Class.forName("com.mysql.cj.jdbc.Driver");
            Connection conn = DriverManager.getConnection(url, user, password);
            System.out.println("Connxion reussite");
            return conn;
 
        } catch (ClassNotFoundException | SQLException e) {
            Logger.getLogger(dbConnection.class.getName()).log(Level.SEVERE, null, e);
            e.printStackTrace();
            throw new SQLException("Erreur lors de la connexion � la base de donnees", e);
        }
    }
*/
}
